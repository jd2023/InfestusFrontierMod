# Planning correction

Own the blocked engineering task. Read repository instructions, the failure,
runner/review logs and the saved candidate stash named in the parked receipt.
The worktree contains the last accepted code; the failed implementation is safely
retained. Inspect it with git show/diff, not by applying it during planning.

Correct only the supplied planning paths. Resolve dependencies, interface contracts,
initial numbers, test commands and exact edit scopes. Split oversized pending work
using new unique task IDs. Preserve accepted packets and the blocked task's ID.
Update content-plan.json ownership and producer edges with each split. Preserve
accepted obligations and explicit exclusions; a task cannot silently drop a rank
or substitute an unavailable ingredient. Qualification profiles follow a tested
harness and must fit the configured execution budgets.
Do not remove gameplay obligations, weaken assertions, change models/permissions,
edit acceptance machinery, implement gameplay, commit, push or discard a stash.

If the packet is already correct, return retry with the concrete code-level
diagnosis and regression assertion; no document change is required. A revised plan
must remain consistent with its owning gameplay specifications. Only genuine new
product choices or external authority warrant external. No numerical assignments
to the user. Return revised after edits, retry for a precise repair diagnosis,
or external with the exact missing authority. Independent review and delivery are
owned by the launcher, not this session.
